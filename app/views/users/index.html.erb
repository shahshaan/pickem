<ul>
  <% @users.each do |user| %>
    <li>
      (<%= user.id %>) <%= link_to user.full_name, user_path(user.id) %>
    </li>
  <% end -%>
</ul>


<% Matchup.all_finished.each do |matchup| %>
  <%= matchup.display %>
  <%= matchup.winner.name %>
  <%# current_user.picks.where(matchup_id: matchup.id).each do |pick| %>
    <p><%#= pick.team.name %></p>
  <%# end %>
<% end %>

<%#= current_user.score %>